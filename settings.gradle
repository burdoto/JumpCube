rootProject.name = 'JumpCube'

var dirApi = file('../../java/japi/')
if (!dirApi.exists())
    dirApi = file('../api')
if (dirApi.exists()) {
    include(':japi')
    project(':japi').projectDir = dirApi
}
